# CLAUDE.md

The full contributor and AI-agent guide for this repository lives in
**[AGENTS.md](./AGENTS.md)** — read it before making changes.

It covers the repository map, the critical constraints (don't move
`server/data/`, `server/data-dist/` is a deliberate redistribution exception,
the intentional `github.com/StarLoco/arena-2.70` module path, the studio
`frontend/dist` embed placeholder, no Git LFS), and the build / test / run
commands.

For the human-facing project overview, see [README.md](./README.md).
